import React from 'react';
import { Header } from '../../antd/index';

const HeaderDefault = () => {
    return (
        <Header>
            Header
        </Header>
    );
};

export default HeaderDefault;
